package net.iubris.optimus_saint.model.saint.data;

public class Lane extends AbstractValue {

	/*@Override
	StatsValue findByInternalEnum(JsonObject jsonObject, String what, String localization) {
		return LaneEnum.valueOf( buildEnumeration(jsonObject, what, localization) );
	}
	
	private static enum LaneEnum implements StatsValue {
		FRONT {
			@Override
			public String getLocalized(Localization localization) {
				return _frontMap.get(localization);
			}
		}
		;
		private static Map<Localization,String> _frontMap = new HashMap<>();
		static {
			_frontMap.put(Localization.EN, "Front");
		}
	}*/
	
	
}
